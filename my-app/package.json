{
  "name": "my-app",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:dev --prefix back\" \"npm run dev --prefix front\"",
    "install-all": "npm install --prefix back && npm install --prefix front",
    "install-concurrently": "npm install concurrently --save-dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}